

    

fokhrul@fokhrul-VirtualBox:~/Desktop$ ./hello.sh
ubuntu windows kali
ubuntu windows kali mac
centOS windows kali mac
windows
0 1 2 3
4
centOS windows mac
0 1 3       ; In bash escaping some indices is allowed. we have 0 and 1. 
            ; We don't have the second index. Then we got the third index.
fokhrul     ;If we treat a string as an array, then this array has a single element
fokhrul     ; at the zeroth index.
0           ;Look, the length of this array is 0



